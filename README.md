# Express + Prisma + TypeScript API

A simple, type-safe backend API built with **Express.js**, **Prisma ORM**, and **TypeScript**. It allows you to manage `users` and their `todos` with full relational data support.

---

## Features

- REST API for users and todos
- Type-safe database operations with Prisma
- Relational data fetching (`user → todos`)
- Clean Express.js routing
- Ready for production with `.env` support

---

## Tech Stack

- **Node.js**
- **Express.js**
- **Prisma ORM**
- **TypeScript**
- **SQLite** (default, easily switchable to PostgreSQL/MySQL)




